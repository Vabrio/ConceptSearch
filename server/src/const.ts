// DB communication
const DB_LOCATION = '../db/CS.sqlite';


// Extracting parameters
const EXTRACT_SEPARATOR = ". "; // 2 sentences are separated with "."
const EXTRACT_SIZE = 1; // number of sentences

const MAX_PER_WRITING = 2; // number of extracts per writing
const MAX_PER_AUTHOR = 2; // Number of writinges per author


export{DB_LOCATION, EXTRACT_SEPARATOR, EXTRACT_SIZE, MAX_PER_WRITING, MAX_PER_AUTHOR}