class Manager {
    
    static getWriting (id: string){
        // Get the writing and associated concepts
        return "TOBEDONE";
    }
    
    static addConcept(concept: string, writingRef: string, firstCh: string, lastCh: string){
        // Add a concept to dataBase, return true if no problem
        return true;
    }
}

export {Manager};