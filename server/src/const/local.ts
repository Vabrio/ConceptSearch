//------------------------------------------------------------------
// Constants that are local for each system (file is in gitignore)
//------------------------------------------------------------------
// Show the console (true) or put in a file  (false)
const LOG_FILE = true;

// Server values
const PORT = 8080;
const SSL_KEY = "path_to_ssl_key";
const SSL_CERTIFICATE = "path_to_ssl_certificate";
const USE_SSL = false;

export { LOG_FILE, PORT, SSL_KEY, SSL_CERTIFICATE, USE_SSL}
